{
  "extends": "./tsconfig.json",
  "comment": "prisma/ is excluded so that seed.ts does not widen the inferred rootDir past src/, which would emit dist/src/main.js and break the Dockerfile CMD. `prisma db seed` runs it through ts-node with tsconfig.json instead.",
  "exclude": ["node_modules", "test", "dist", "prisma", "**/*spec.ts"]
}
